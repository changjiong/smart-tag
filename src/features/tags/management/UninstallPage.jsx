import React from 'react';
import { createPlaceholderPage } from '../../../components/Common/PageFactory';

/**
 * 标签卸载页面
 * 安全移除不再使用的标签
 */
const UninstallPage = createPlaceholderPage('标签卸载', '安全移除不再使用的标签');

export default UninstallPage;
