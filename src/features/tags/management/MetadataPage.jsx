import React from 'react';
import { createPlaceholderPage } from '../../../components/Common/PageFactory';

/**
 * 标签元数据页面
 * 管理标签的元数据属性
 */
const MetadataPage = createPlaceholderPage('标签元数据', '管理标签的元数据属性');

export default MetadataPage;
