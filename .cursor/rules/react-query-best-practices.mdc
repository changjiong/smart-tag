---
description: 
globs: **/*.js,**/*.jsx
alwaysApply: false
---


- Use `useQuery` for fetching data and `useMutation` for side effects
- Implement proper error handling and loading states
- Utilize query keys effectively for caching and refetching
- Use `queryClient` for manual cache management when necessary